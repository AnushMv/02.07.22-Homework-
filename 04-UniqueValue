function getUniqueValue(arr){
    let uniqueValueArr = [];
    for(let k = 0, j= 0, e = 0; k < arr.length; k++){
        for(let i = 1; i < arr.length; i++){
            if(arr[j] !== arr[i]){   
               j++;
            }else{
                break;
            } 
            uniqueValueArr[e] = arr[j];
        }
    }
    return uniqueValueArr;
}
